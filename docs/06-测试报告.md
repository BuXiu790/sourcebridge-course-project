# 06 测试报告

## 1. 测试信息

- 项目版本：SourceBridge Course Release v1.0
- 测试日期：2026-07-11
- 本地环境：Windows、Node.js 24、npm、Vinext/Vite
- 真实环境：Supabase PostgreSQL + Auth + RLS、OpenAI Sites

## 2. 自动化与构建结果

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| ESLint | `npm run lint` | 通过，0 Error |
| TypeScript | `npx tsc --noEmit` | 通过 |
| 自动化测试 | `npm test` | 14/14 通过 |
| 生产构建 | `npm run build`（由 test 执行） | 通过，公开、Auth、Buyer、Admin、API 路由生成成功 |
| 生产依赖安全 | `npm audit --omit=dev` | 0 个已知漏洞 |
| 真实 Supabase | `npm run test:live` | 通过；普通会话、无 service role |
| Git 敏感信息 | 高置信度模式扫描 | 0 项 |

## 3. 14 项自动化测试覆盖

1. 首页、Demo、登录、暂停注册、找回密码公开渲染。
2. 匿名访问受保护页面跳转登录。
3. 匿名 API 写入在配置/未配置状态返回 401/503。
4. 无 Starter Preview 残留。
5. Supabase 未配置时权限安全关闭。
6. Supabase 已配置但匿名时拒绝访问。
7. 公开与已认证权限决策。
8. 完整 RFQ 校验和规范化。
9. 未选产品分类校验。
10. URL、日期等无效输入校验。
11. 六张业务表均启用 RLS。
12. 新用户固定 Buyer、Staff 写操作受保护。
13. 服务端鉴权与 Demo/真实数据分离。
14. 浏览器 Bundle 不含 service role 凭据。

## 4. 真实 RLS 权限矩阵

| 操作 | 匿名 | Buyer A | Buyer B | Operator | Admin |
| --- | --- | --- | --- | --- | --- |
| 读取业务表 | 拒绝 | 仅自己的关联数据 | 仅自己的关联数据 | 全部业务数据 | 全部业务数据 |
| 创建 RFQ | 拒绝 | 自己 | 自己 | 允许 | 允许 |
| 读取 Buyer A RFQ | 拒绝 | 允许 | 拒绝 | 允许 | 允许 |
| 创建/编辑 Supplier | 拒绝 | 拒绝 | 拒绝 | 允许 | 允许 |
| 添加 Quote/Timeline | 拒绝 | 拒绝 | 拒绝 | 允许 | 允许 |
| 选择自己的 Quote | 拒绝 | 允许 | 不可操作 A | Staff 可操作 | Staff 可操作 |
| 修改角色 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 允许 |

真实验收保留一条 Buyer A 完整课程记录：两份匿名报价、已选择报价、附件元数据、打样/生产/质检/运输时间线。脚本临时创建的 Supplier、Quote、Timeline 与状态变化均已清理或恢复。

## 5. 浏览器与响应式

| 视口/场景 | 结果 |
| --- | --- |
| 390px 首页、公开 Demo、Login | 无横向溢出，CTA 和表单可见 |
| 390px Dashboard、New RFQ、详情 | 无横向溢出；标题未被 Sticky Header 遮挡 |
| 390px 多步骤表单 | 分类初始未选；错误清晰；日期输入可进入 Review |
| 1440px 首页、Dashboard、详情、Admin | 双栏/表格布局正常 |
| 报价选择 | 状态更新，成本重新计算；真实 RFQ 刷新后持久化 |
| 键盘与焦点 | 原生标签、按钮、Select 可操作；`focus-visible` 样式存在 |
| 控制台 | 无 SourceBridge 应用错误；网络请求成功 |

## 6. 安全检查

- `.env.local`、`.private/`、测试账号和普通会话文件均被 `.gitignore` 排除。
- Git 未跟踪上述路径。
- 代码和构建 Bundle 不含密码、数据库密码、service role key 或真实密钥。
- Live 脚本不读取用户密码；会话来自本机受保护文件。
- RLS 始终开启，未通过关闭 RLS 绕过测试。
- Course Release v1.0 不公开账号密码，不开放公众注册。

## 7. 结论

本版本满足课程演示所需的功能完整性、组件化、数据库、接口、权限、响应式、安全、测试和部署证据。商业化功能与自定义 SMTP 明确排除在课程版本之外。
